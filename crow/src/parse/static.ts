/**
 * @license MIT (see project's LICENSE file)
 */

import { NotImplemented } from '@tiny/core';
import { IStaticInput } from '../types';

export function parseStaticInput(input: string): IStaticInput {
	throw new NotImplemented();
}
